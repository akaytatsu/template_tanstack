services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile-dev
    ports:
      - "5173:5173"
    volumes:
      - ./app:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
