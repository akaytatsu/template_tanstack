# Dependencies
node_modules
app/node_modules

# Build output
dist
dist-ssr
app/dist
app/.output
app/.nitro
app/.tanstack
app/coverage

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environment files (contain secrets / API URLs)
.env
.env.local
.env.*.local
app/.env
app/.env.local
app/.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.local

# TypeScript
*.tsbuildinfo

# Tooling
.claude/

# NOTE: app/app/routeTree.gen.ts is intentionally committed — the build runs
# `tsc -b` before Vite regenerates it, so a fresh clone needs it present.
